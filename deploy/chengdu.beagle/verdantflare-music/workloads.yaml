apiVersion: apps/v1
kind: Deployment
metadata:
  name: music-minimax-music3-api
spec:
  replicas: 1
  progressDeadlineSeconds: 3600
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: music-minimax-music3-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: music-minimax-music3-api
    spec:
      hostIPC: true
      nodeSelector:
        kubernetes.io/hostname: 10.241.109.7
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: nvidia.com/gpu.product
                    operator: In
                    values:
                      - NVIDIA-GeForce-RTX-4090
                  - key: nvidia.com/gpu.sharing-strategy
                    operator: In
                    values:
                      - none
      containers:
        - name: api
          image: registry.cn-qingdao.aliyuncs.com/wod/verdantflare-app:music-minimax-music3-api-v0.1.1
          imagePullPolicy: IfNotPresent
          args:
            - --mem-fraction-static
            - "0.80"
          env:
            - name: HF_ENDPOINT
              value: https://hf-mirror.com
            - name: HF_HUB_DISABLE_XET
              value: "1"
          ports:
            - name: http
              containerPort: 8000
          resources:
            requests:
              cpu: "16"
              memory: 64Gi
              nvidia.com/gpu: "2"
            limits:
              cpu: "32"
              memory: 128Gi
              nvidia.com/gpu: "2"
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 120
            periodSeconds: 30
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 3
            periodSeconds: 30
            timeoutSeconds: 5
          volumeMounts:
            - name: models
              mountPath: /models/MiniMax-Music3
            - name: shared-memory
              mountPath: /dev/shm
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: music3-models
        - name: shared-memory
          emptyDir:
            medium: Memory
            sizeLimit: 32Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: music-uvr5-api
spec:
  replicas: 1
  progressDeadlineSeconds: 10800
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: music-uvr5-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: music-uvr5-api
    spec:
      nodeSelector:
        kubernetes.io/hostname: 10.241.109.7
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: nvidia.com/gpu.product
                    operator: In
                    values:
                      - NVIDIA-GeForce-RTX-4090
                  - key: nvidia.com/gpu.sharing-strategy
                    operator: In
                    values:
                      - none
      containers:
        - name: api
          image: registry.cn-qingdao.aliyuncs.com/wod/verdantflare-app:music-uvr5-api-v0.1.7
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
          resources:
            requests:
              cpu: "8"
              memory: 24Gi
              nvidia.com/gpu: "1"
            limits:
              cpu: "16"
              memory: 48Gi
              nvidia.com/gpu: "1"
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 720
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 3
            periodSeconds: 30
            timeoutSeconds: 5
          volumeMounts:
            - name: models
              mountPath: /models/audio-separator
            - name: temporary
              mountPath: /tmp/uvr5
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: uvr5-models
        - name: temporary
          emptyDir:
            sizeLimit: 20Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: music-rvc-api
spec:
  replicas: 1
  progressDeadlineSeconds: 3600
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: music-rvc-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: music-rvc-api
    spec:
      nodeSelector:
        kubernetes.io/hostname: 10.241.109.7
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: nvidia.com/gpu.product
                    operator: In
                    values:
                      - NVIDIA-GeForce-RTX-4090
                  - key: nvidia.com/gpu.sharing-strategy
                    operator: In
                    values:
                      - none
      containers:
        - name: api
          image: registry.cn-qingdao.aliyuncs.com/wod/verdantflare-app:music-rvc-api-v0.1.5
          imagePullPolicy: IfNotPresent
          env:
            - name: HF_ENDPOINT
              value: https://hf-mirror.com
          ports:
            - name: http
              containerPort: 8000
          resources:
            requests:
              cpu: "16"
              memory: 48Gi
              nvidia.com/gpu: "1"
            limits:
              cpu: "32"
              memory: 96Gi
              nvidia.com/gpu: "1"
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 60
            periodSeconds: 20
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 3
            periodSeconds: 30
            timeoutSeconds: 5
          volumeMounts:
            - name: models
              mountPath: /models/rvc
            - name: temporary
              mountPath: /tmp/rvc
            - name: shared-memory
              mountPath: /dev/shm
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: rvc-models
        - name: temporary
          emptyDir:
            sizeLimit: 100Gi
        - name: shared-memory
          emptyDir:
            medium: Memory
            sizeLimit: 8Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: music-lyrics-aligner-api
spec:
  replicas: 1
  progressDeadlineSeconds: 1800
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: music-lyrics-aligner-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: music-lyrics-aligner-api
    spec:
      nodeSelector:
        kubernetes.io/hostname: 10.241.109.7
      affinity:
        nodeAffinity:
          requiredDuringSchedulingIgnoredDuringExecution:
            nodeSelectorTerms:
              - matchExpressions:
                  - key: nvidia.com/gpu.product
                    operator: In
                    values:
                      - NVIDIA-GeForce-RTX-4090
                  - key: nvidia.com/gpu.sharing-strategy
                    operator: In
                    values:
                      - none
      containers:
        - name: api
          image: registry.cn-qingdao.aliyuncs.com/wod/verdantflare-app:music-lyrics-aligner-api-v0.1.0
          imagePullPolicy: IfNotPresent
          env:
            - name: LYRICS_ALIGNER_MODEL_ROOT
              value: /models/whisper
            - name: LYRICS_ALIGNER_MODEL
              value: small
            - name: LYRICS_ALIGNER_LANGUAGE
              value: zh
            - name: LYRICS_ALIGNER_TEMP_ROOT
              value: /tmp/lyrics-aligner
          ports:
            - name: http
              containerPort: 8000
          resources:
            requests:
              cpu: "4"
              memory: 16Gi
              nvidia.com/gpu: "1"
            limits:
              cpu: "8"
              memory: 32Gi
              nvidia.com/gpu: "1"
          startupProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 120
            periodSeconds: 10
            timeoutSeconds: 5
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 3
            periodSeconds: 30
            timeoutSeconds: 5
          volumeMounts:
            - name: models
              mountPath: /models/whisper
            - name: temporary
              mountPath: /tmp/lyrics-aligner
      volumes:
        - name: models
          persistentVolumeClaim:
            claimName: lyrics-aligner-models
        - name: temporary
          emptyDir:
            sizeLimit: 10Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: music-audio-mixer-api
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: music-audio-mixer-api
  template:
    metadata:
      labels:
        app.kubernetes.io/name: music-audio-mixer-api
    spec:
      nodeSelector:
        kubernetes.io/hostname: 10.241.109.7
      containers:
        - name: api
          image: registry.cn-qingdao.aliyuncs.com/wod/verdantflare-app:music-audio-mixer-api-v0.1.0
          imagePullPolicy: IfNotPresent
          ports:
            - name: http
              containerPort: 8000
          resources:
            requests:
              cpu: "2"
              memory: 4Gi
            limits:
              cpu: "8"
              memory: 16Gi
          volumeMounts:
            - name: temporary
              mountPath: /tmp/music-mixer
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 3
            periodSeconds: 30
            timeoutSeconds: 5
      volumes:
        - name: temporary
          emptyDir:
            sizeLimit: 20Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: music-mcp-server
spec:
  replicas: 1
  selector:
    matchLabels:
      app.kubernetes.io/name: music-mcp-server
  template:
    metadata:
      labels:
        app.kubernetes.io/name: music-mcp-server
    spec:
      nodeSelector:
        kubernetes.io/hostname: 10.241.109.7
      containers:
        - name: api
          image: registry.cn-qingdao.aliyuncs.com/wod/verdantflare-app:music-mcp-server-v0.5.2
          imagePullPolicy: IfNotPresent
          env:
            - name: MUSIC_ARTIFACT_ROOT
              value: /data/projects
            - name: MUSIC_ASSET_IMPORT_ORIGINS
              value: https://cache.ali.wodcloud.com
            - name: MUSIC_MCP_PUBLIC_BASE_URL
              value: https://mcp.cn-chengdu.bc-cloud.com/music
            - name: MUSIC_MCP_BEARER_TOKEN
              valueFrom:
                secretKeyRef:
                  name: music-mcp-auth
                  key: MUSIC_MCP_BEARER_TOKEN
            - name: MUSIC_MCP_ALLOWED_HOSTS
              value: mcp.cn-chengdu.bc-cloud.com,127.0.0.1:*,localhost:*,[::1]:*
            - name: MUSIC_MCP_ALLOWED_ORIGINS
              value: https://mcp.cn-chengdu.bc-cloud.com,http://127.0.0.1:*,http://localhost:*,http://[::1]:*
            - name: MUSIC3_URL
              value: http://music-minimax-music3-api:8000
            - name: UVR5_URL
              value: http://music-uvr5-api:8000
            - name: RVC_URL
              value: http://music-rvc-api:8000
            - name: LYRICS_ALIGNER_URL
              value: http://music-lyrics-aligner-api:8000
            - name: MIXER_URL
              value: http://music-audio-mixer-api:8000
          ports:
            - name: http
              containerPort: 8000
          resources:
            requests:
              cpu: 250m
              memory: 256Mi
            limits:
              cpu: "2"
              memory: 2Gi
          volumeMounts:
            - name: projects
              mountPath: /data/projects
          readinessProbe:
            httpGet:
              path: /health
              port: http
            periodSeconds: 10
            timeoutSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: http
            failureThreshold: 3
            periodSeconds: 30
            timeoutSeconds: 5
      volumes:
        - name: projects
          persistentVolumeClaim:
            claimName: music-projects
